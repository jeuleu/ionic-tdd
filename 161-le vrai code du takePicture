- écrire le vrai code du takePicture

>> ERROR  Unknown provide: CameraSrvProvider <- CameraSrv <- CameraController


 //       vm.takePicture = function() {

           var options = { 
                quality : 75, 
                allowEdit : false,
                targetWidth: 300,
                targetHeight: 300,
                saveToPhotoAlbum: false
            };

            CameraSrv.getPicture(options).then(function(resultImg) {
              console.log(resultImg);
              vm.imageURI = resultImg;
            }, function(err) {
            
              console.log("takePicture: " + err);
              
              vm.imageURI = "img/ionic.png";
            });

//        }

